# TODO: Modern Frontend Tooling

## Done

- [x] Vue 3 + Vite + TypeScript
- [x] Tailwind CSS 4 + PrimeVue (Aura) + tailwindcss-primeui
- [x] Pinia (state management)
- [x] Vue Router
- [x] ESLint (oxlint + Vue recommended + Prettier integration)
- [x] Prettier + format script
- [x] Vitest (unit testing)
- [x] VueUse
- [x] EditorConfig, VS Code extensions
- [x] consola (structured logging)

## To Do

- [ ] **husky + lint-staged** — auto-run lint/format on commit, no bad code gets in
- [ ] **commitlint** — enforce conventional commits (`feat:`, `fix:`, etc.)
- [ ] **Playwright** (test framework) — E2E tests with `@playwright/test`
- [x] **unplugin-vue-components** — auto-register PrimeVue components, no manual imports
- [x] **unplugin-auto-import** — auto-import `ref`, `computed`, `useRouter`, etc.
- [ ] **.nvmrc** — pin Node version for team consistency
- [ ] **VS Code settings.json** — format-on-save, ESLint auto-fix
- [ ] **GitHub Actions CI** — lint + type-check + test on every PR
- [ ] **Sentry** — error tracking and performance monitoring in production
- [x] **ofetch** — modern HTTP client (auto JSON, retries, interceptors), replace raw `fetch`
- [x] **TanStack Vue Query** — data fetching/caching layer (loading states, refetching, stale-while-revalidate)
- [ ] **rollup-plugin-visualizer** — bundle size analysis
- [ ] **unplugin-vue-router** — type-safe file-based routing
- [ ] **vue-i18n** — internationalization
- [ ] **Storybook** — component development and documentation in isolation
